Installation of Linux Distribution

Step1:Ubuntu
Step2:Download latest LTS
Step3:Open VM Ware Player
Step4:Create New Virtual Machine(select the iso manually)
Step5:Browse the ISO File
Step6:Enter Information (Create Username and Password)
Step7:Click Finish 
Step8:Next --> Next
Step9:Install Now
Step10:Continue
Step11: Installation Complete

----------------------------------------------------------------------------------------------------------------
Commands
1)date (displays the current date, time and day).
2)echo statement (used as print statement in Linux).
3)ls (list of directories).
4)pwd (present working directory).
5)mkdir (create directory).
6)cd (to go into created directory).
7)cd\ (to get out of directory).
8)rmdir (to remove directory).
9)touch (to create empty file).
10)who (to check the current host).
11)cal ‘year’ (to display calendar).

----------------------------------------------------------------------------------------------------------------
Shell Scripting 

Run the Programs:
>touch filename.sh //create empty file
//Open that file
//Paste the Program
>./filename.sh
//for permission
>chmod 777 filename.sh

******************************************************************
1)Calculator sell script: +,-,*,/,%.
CODE:
echo -e “Enter two numbers:”;
read a b;
echo -e “a+b = `expr $a + $b`”;
echo -e “a-b = `expr $a - $b`”;
echo -e “a*b = `expr $a * $b`”;
echo -e “a/b = `expr $a / $b`”;
echo -e “a%b = `expr $a % $b`”;
******************************************************************

2)Positive, Negative or Zero
CODE:
echo -e "Enter a number:";
read a
if [ $a -gt 0 ]
then echo -e "$a is positive";
elif [ $a -lt 0 ]
then echo -e "$a is negative";
else
echo -e "$a is equal to 0";
fi
******************************************************************
3)even or odd.
CODE:
echo -e “Enter a number”;
read a
if [ `expr $a % 2` == 0 ]
then echo -e “$a is even”;
else
echo -e “$a is odd”;
fi
******************************************************************
4)Write a shell script to find the greater number between two.
CODE:
echo -e “Enter two numbers:”;
read a b
if [ $a -gt $b ]
then echo -e “$a is greater”;
else
echo -e “$b is greater”;
fi
******************************************************************
5)Write a shell script to check whether the entered number is
divisible by 3,5 or 7.
CODE:
echo -e "Enter a number:";
read a

if [ $((a % 3)) -eq 0 ]; then
    echo -e "$a is divisible by 3";
elif [ $((a % 5)) -eq 0 ]; then
    echo -e "$a is divisible by 5";
elif [ $((a % 7)) -eq 0 ]; then
    echo -e "$a is divisible by 7";
else
    echo -e "Error: $a is not divisible by 3, 5, or 7";
fi
******************************************************************
6)Write a shell script which reads 2 numbers a and b and
check whether a is smaller than or greater than b or equal to b.
CODE:
echo -e “Enter teo numbers:”;
read a b
if [ $a -gt $b ]
then echo -e “$a is greater”;
elif [ $a -lt $b ]
then echo -e “$a is smaller”;
elif [ $a == $b ]
then echo -e “$a is equal to $b”;

else
echo -e “Invalid”;
fi
******************************************************************
7)Write a shell script to print week day name of the week
corresponding to week day number.Give apprropriate message
if the number is not between 1 to 7.
CODE:
echo -e "Enter day number:";
read a

case $a in
    1) echo -e "Sunday";;
    2) echo -e "Monday";;
    3) echo -e "Tuesday";;
    4) echo -e "Wednesday";;
    5) echo -e "Thursday";;
    6) echo -e "Friday";;
    7) echo -e "Saturday";;
    *) echo -e "Invalid output! Kindly enter a number between 1 to 7";;
esac
******************************************************************
8)11] Using case, write a shell script to find out the entered
character is vowel or not.
CODE:
echo -e “Enter character”;
read a
case $a in
a) echo -e “VOWEL”;;

e) echo -e “VOWEL”;;
i) echo -e “VOWEL”;;
o) echo -e “VOWEL”;;
u) echo -e “VOWEL”;;
A) echo -e “VOWEL”;;
E) echo -e “VOWEL”;;
I) echo -e “VOWEL”;;
O) echo -e “VOWEL”;;
U) echo -e “VOWEL”;;
*) echo -e “Invalid Input”;;
esac
-----------------------------------------------------------------------------
9) Write a shell script to enter the number from user for choice
1 for +, 2 for -, 3 for *.Get two numbers again from the user and
calculate the result.
CODE:
echo -e “Enter number between 1 and 3”;
read a
echo -e “Enter two numbers”;
read b c
case $a in
1) echo -e “b+c = `expr $b + $c`”;;
2) echo -e “b-c = `expr $b - $c`”;;
3) echo -e “b*c= `expr $b \* $c`”;;
*) echo -e “Invalid Input”;;
esac

-----------------------------------------------------------------------------
10)Write a shell script to read a character n check whether it is
upper case letter or lower case letter or digit or any other
special character and give appropriate message.
CODE:
echo -e “Enter a character : “;
read n
case $n in
[A-Z]) echo -e “$n is an upper-case alphabet”;;
[0-9]) echo -e “$n is a digit”;;
[a-z]) echo -e “$n is a lower-case alphabet”;;
*) echo -e “$n is a special character”;;
esac

-----------------------------------------------------------------------------
14] Write a shell script to print sum of n number using for and
while loop.
CODE:
# For Loop Version
echo -e "Enter size:";
read n
sum=0
echo -e "Enter numbers:";
for ((i=1; i <= n; i++))
do
    read num
    sum=$((sum + num))  # Using $(( )) for arithmetic
done
echo -e "Sum is $sum"

# While Loop Version
echo -e "Enter size:";
read n
sum=0
i=1
echo -e "Enter numbers:";
while [ $i -le $n ]
do
    read num
    sum=$((sum + num))  # Using $(( )) for arithmetic
    i=$((i + 1))        # Incrementing i
done
echo -e "Sum is $sum"
-----------------------------------------------------------------------------
15)Write a shell script to read marks in 5 subjects and find the
percentage and

a) If percentage is less than 35 then fail.
b) If percentage is greater than 35 and less than 45 then grade
C.
c) If percentage is greater than 45 and less than 60 then grade
B.
d) If percentage is greater than 45 and less than 75 then grade
A.
e) If percentage is greater than 75 then distinction.

CODE:
echo "Enter five subject marks.";
read a b c d e;
total=`expr $a + $b + $c + $d + $e`
per=`expr $total / 5`
echo "Your percentage is $per%.";
if [ $per -lt 35 ]
then echo "You have failed.";
elif [ $per -ge 35 -a $per -lt 45 ]
then echo "Grade C";
elif [ $per -ge 45 -a $per -lt 60 ]
then echo "Grade B";
elif [ $per -ge 60 -a $per -lt 75 ]
then echo "Grade A";
else echo "Distinction";
fi
-----------------------------------------------------------------------------

16)Write a shell script to display the number of users currently
logged in the system, display the present working
directory, and display the calendar of your own birth year
and display the today’s date and time.
Code:-
echo -e “No. of users in the system : `who` ”;
echo -e “Present working directory : `pwd`”;
echo -e “Calendar of birth month : `cal 06 2004` “;
echo -e “Current date and time : `date` “;
--------------------------------------------------------
17) Write a shell scipt :
 To create a directory.  Move the content of one file to another file
 Display the list of directories and files.
 To remove a directory
CODE:
echo -e “To create directory : `mkdir prac`”;
echo -e “To move content : `mv SAROJ das`”;
echo -e “To display list of directory : `ls`”;
echo -e “To remove directory : `rmdir prac`”;


----------------------------------------------------------------------------------------------------------------
C Programs

Run the Programs:
>touch filename.c //create empty file
//Open that file
//Paste the Program
>gcc filename.c -o program
>./filename.c
//for permission
>chmod 777 filename.c


1)Write a C program to print sum of two numbers.
CODE:
#include<stdio.h>
int main()
{
    int a, b, c;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    c = a + b;
    printf("Sum is %d\n", c);
    return 0;
}
--------------------------------------------------------------------
2) Write a c program to find even and odd number
using C programming.
CODE:
#include<stdio.h>
int main()
{
    int a;
    printf("Enter a number: ");
    scanf("%d", &a);
    if (a % 2 == 0) {
        printf("Number is even: %d\n", a);
    } else {
        printf("Number is odd: %d\n", a);
    }
    return 0;
}
----------------------------------------------------------------
3)Finding maximum between three number using C
Programming.
CODE:
#include<stdio.h>
int main()
{
    int a, b, c;
    printf("Enter first number: ");
    scanf("%d", &a);
    printf("Enter second number: ");
    scanf("%d", &b);
    printf("Enter third number: ");
    scanf("%d", &c);
    if (a > b) {
        if (a > c) {
            printf("%d is max\n", a);
        } else {
            printf("%d is max\n", c);
        }
    } else {
        if (b > c) {
            printf("%d is max\n", b);
        } else {
            printf("%d is max\n", c);
        }
    }
    return 0;
}

----------------------------------------------------------------
4)Find the factorial of number using C programming.
CODE:
#include <stdio.h>
int main()
{
    int i, fact = 1, num;
    printf("Enter a number: ");
    scanf("%d", &num);
    for (i = 1; i <= num; i++) {
        fact = fact * i;
    }
    printf("Factorial of %d is %d\n", num, fact);
    return 0;
}

----------------------------------------------------------------
5)5. Write a C program  sum of two numbers.
CODE:
#include<stdio.h>
void main()
{
    int a, b, c;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    c = a + b;
    printf("Sum of %d and %d is %d\n", a, b, c);
}
----------------------------------------------------------------
6)program to print table of a number
#include <stdio.h>

int main() {
    int number, i;

    // Prompt user for the number
    printf("Enter a number to print its multiplication table: ");
    scanf("%d", &number);

    // Print the multiplication table
    printf("\nMultiplication Table of %d:\n", number);
    for (i = 1; i <= 10; i++) {
        printf("%d x %d = %d\n", number, i, number * i);
    }

    return 0;
}




----------------------------------------------------------------------------------------------------------------
Working with users, groups and permissions.

Step 1: mkdir tyith [make directory]
Step 2:  cd tyith [go to directory]
Step 3:  pwd [path in which directory]
Step 5:  touch text.txt [create file]
Step 6:  touch harry.txt [create file]
Step 7:  ls [list of files]
Step 8:  chmod 700 text.txt [Give permission]
Step 9:  pwd [path of directory]
Step 10:  chmod 764 text.txt [Permissions] 
Step 11:  ls -l text.txt [check permission are done]
//Create user
sudo adduser username
//Create group
sudo addgroup groupname
//add user to group
sudo usermod -aG groupname username
//update password for user
sudo passwd username
//print
id username

----------------------------------------------------------------------------------------------------------------
SSH Server: Password Authentication

sudo apt-get update
//provide password which you have created during installation of ubuntu
sudo apt-get install openssh-server
Sudo systemctl start sshd
Sudo systemctl status sshd
ifconfig
Copy the inet address(192.168.0.109)
and ping it in your windows command prompt
Install Putty (For installing putty app)
https://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html
64-bit x86: putty.exe
//open putty provide IP address click on open > accept > enter your credential of linux
Hostname (or IP address)>inet address (192.168.29.194) >Open
Select Accept
//Put your Ubuntu login credentials
//Login as your username and password
//Run basic Linux commands in putty
ls
pwd
tcpdump -e command
//Press Ctrl+C


----------------------------------------------------------------------------------------------------------------
Installing and Configuring FTP Server
//Install FTP Server
sudo apt update
sudo apt install vsftpd
sudo systemctl restart vsftpd
ifconfig
ftp hostname@ipaddress 
//mention in command prompt eg . Ubuntu
Go to your windows file explorer > Network > ftp://ubuntu@ipaddress/

----------------------------------------------------------------------------------------------------------------
Install Samba to share folders or files between Windows and Linux
Step 1: sudo apt-get update
Step 2: sudo apt install samba
Step 3: sudo mkdir -p /home/somaiya/somaiya_TYIT
Step 4: sudo cp /etc/samba/smb.conf /etc/samba/smb.conf.backup
Step 5: cd /home/somaiya
Step 6: sudo adduser somaiya
Step 7: sudo smbpasswd -a somaiya
Step 8: Restarting the samba services

sudo systemctl restart smbd
sudo systemctl status smbd

step 9: Editing Samba Configuration File

sudo nano /etc/samba/smb.conf

Add this line in last 
[somaiya_TYIT]
Path = /home/somaiya/somaiya_TYIT
Read only=no
Valid users = somaiya

Step 10: ifconfig, copy the IP Address
Step 11: Now go to Windows Explorer

Go to File explorere type the ip address of ubantu OS to access the folder [folder_name] double click on the folder
OR
Go to your windows file explorer > Network > \\ipaddress\somaiya_TYIT


----------------------------------------------------------------------------------------------------------------
//xampp--simple connection
<?php
$conn = mysqli_connect("localhost", "root", "", "TYITH");

if (!$conn) {
    die("Connection failed: " . mysqli_connect_error());
} else {
    echo "Connection successful!";
}
?>

----------------------------------------------------------------------------------------------------------------
//xampp login with register
<?php
// Database Connection
$con = mysqli_connect('localhost', 'root', '', 'TYITH'); //database TYITH
if (!$con) {
    die("Connection failed: " . mysqli_connect_error());
}

// Handle Login Form Submission
if (isset($_POST['login_submit'])) {
    $username = $_POST['username'];
    $password = $_POST['pass'];

    $query = "INSERT INTO loginpage(UserName, Password) VALUES ('$username', '$password')";
    $execute = mysqli_query($con, $query);

    if ($execute) {
        // Move to Registration Page (Same File)
        $showRegistration = true;
    } else {
        echo "Error: " . mysqli_error($con);
    }
}

// Handle Registration Form Submission
if (isset($_POST['register_submit'])) {
    $name = $_POST['name'];
    $email = $_POST['email'];
    $mobile = $_POST['mobile'];

    $query = "INSERT INTO registrationpage(Name, Email, Mobile) VALUES ('$name', '$email', '$mobile')";
    $execute = mysqli_query($con, $query);

    if ($execute) {
        echo "<script>alert('Student Registered Successfully!');</script>";
    } else {
        echo "Error: " . mysqli_error($con);
    }
}
?>


<html>
<head>
    <title>Student Portal</title>
</head>
<body>
    <h1>Welcome!</h1>

    <?php if (!isset($showRegistration)) { ?>
        <!-- Login Form -->
        <form method="post">
            UserName: <input type="text" name="username" required><br><br>
            Password: <input type="password" name="pass" required><br><br>
            <input type="submit" name="login_submit" value="Login">
        </form>
    <?php } else { ?>
        <!-- Registration Form (Visible After Login) -->
        <h2>Student Registration</h2>
        <form method="post">
            Name: <input type="text" name="name" required><br><br>
            Email: <input type="email" name="email" required><br><br>
            MobileNo: <input type="text" name="mobile" required><br><br>
            <input type="submit" name="register_submit" value="Register">
        </form>
    <?php } ?>
</body>
</html>




-----------------------------------------------------------------


RiyaThakur -31011022032
  

________________
  

INDEX




Sr No
	Title
	1
	Installation of Linux Distribution
	2
	Shell Scripting and C Programs
	3
	Working with users, groups and permissions.
	4
	SSH Server: Password Authentication
	5
	Installing and Configuring FTP Server
	6
	Install MySQL to configure database server
	7
	Install Samba to share folders or files between Windows and Linux
	8
	Install phpMyAdmin to operate MySQL on a web browser from Clients
	







________________
Practical 01 
Aim: Installation of Linux Distribution


Step 1: Download the Distribution. Here we are using Ubuntu.
Download link is - https://ubuntu.com/download/desktop


Step 2: Open VMVare Player / Workstation Pro.
  



Step 3: Click on Create a New Virtual Machine. Select the Ubuntu iso from the file manager.
  

Step 4: Enter the information.
  



Step 5: Give your virtual machine a name and click on next.
  



Step 6: Allot space to your virtual machine and click on next.
  



Step 7: Click on Finish and wait for the VM to power on.
  



Step 8: Complete the Ubuntu Installation process.
  
___

________________


Practical 02
Aim: Shell Scripting and C Programs
A - Shell Scripting


**********
nano filename.sh
chmod 777 filename.sh
./filename.sh
***********
eg:
nano ss.sh
chmod 777 ss.sh
./ss.sh
============================

1. Write a shell script to display the number of users currently logged in the system, display the present working  directory, and display the calendar of your own birth year and display today's date and time.

Code:

echo -e "No. of users in the system : `who` "; 
echo -e "Present working directory : `pwd`"; 
echo -e "Calendar of birth month : `cal 03 2005`"; 
echo -e "Current date and time : `date` "; 


2. Write a shell script which reads a number and checks whether the number is positive, negative or zero.

Code:

echo -e "enter a number: ";
read a
if [ $a -gt 0 ]
then echo -e "$a is positive";
elif [ $a -lt 0 ]
then echo -e "$a is negative";
else
echo -e "$a is equal to 0";
fi
________________


3. Write a shell script which reads a number and checks whether the number is even or odd.

Code:

echo -e "enter a number: ";
read a
if [ $(expr $a % 2) -eq 0 ]
then
  echo -e "$a is even";
else
  echo -e "$a is odd";
fi

________________


4. Write a shell script to check whether the entered number is divisible by 3,5 or 7. 

Code:

echo -e "enter a number";
read a
if [ $(expr $a % 3) -eq 0 ]
then
  echo -e "$a is divisible by 3";
elif [ $(expr $a % 5) -eq 0 ]
then
  echo -e "$a is divisible by 5";
elif [ $(expr $a % 7) -eq 0 ]
then
  echo -e "$a is divisible by 7";
else
  echo -e "error";
fi

________________


5. Write a shell script which reads 2 numbers a and b and checks whether a is smaller than or greater than b or equal to b.

Code:

echo -e "enter the numbers: ";
read a b
if [ $a -gt $b ]
then echo -e "$a is greater";
elif [ $a -lt $b ]
then echo -e "$a is smaller";
elif [ $a == $b ]
then echo -e "$a is equal to $b";
else
echo -e "invalid";
fi

________________

6. Add, sub, mul, divi

#!/bin/bash

echo "Choose an operation:"
echo "1. Addition"
echo "2. Subtraction"
echo "3. Multiplication"
echo "4. Division"
echo "5. Factorial"
read choice


if [ $choice -eq 5 ]; then
    echo "Enter a number:"
    read num
    fact=1
    for (( i=1; i<=num; i++ ))
    do
        fact=$((fact * i))
    done
    echo "Factorial of $num is $fact"
else
    echo "Enter first number:"
    read a
    echo "Enter second number:"
    read b


    case $choice in
        1)
            result=$((a + b))
            echo "Addition: $a + $b = $result"
            ;;
        2)
            result=$((a - b))
            echo "Subtraction: $a - $b = $result"
            ;;
        3)
            result=$((a * b))
            echo "Multiplication: $a * $b = $result"
            ;;
        4)
            if [ $b -eq 0 ]; then
                echo "Error: Division by zero is not allowed."
            else
                result=$((a / b))
                echo "Division: $a / $b = $result"
            fi
            ;;
        *)
            echo "Invalid choice."
            ;;
    esac
fi

________________


7. Factorial 

# Function to calculate factorial
factorial() {
    num=$1
    fact=1

    for (( i=1; i<=num; i++ )); do
        fact=$((fact * i))
    done

    echo "Factorial of $num is: $fact"
}

# Read input from user
read -p "Enter a number: " number

# Check if the input is a positive integer
if [[ $number =~ ^[0-9]+$ ]]; then
    factorial "$number"
else
    echo "Please enter a valid positive integer."
fi
________________


C Programming

1. Write a C program to print the sum of two numbers.

Code:


#include<stdio.h> 
int main() 
{ 
int a,b ,c; 
printf(“Enter two numbers : “) ;
scanf(“%d %d “,&a ,&b); 
c=a+b; 
printf(“Sum is %d” , c); 
return 0; 
} 

*********
nano filename.c 
ctrl o + enter+ ctrlx
gcc filename.c -o filename
./filename
****************
eg:

nano Ashwin.c
gcc Ashwin.c -o Ashwin
./Ashwin
________________


2. Write a shell script program to find even and odd numbers using C programming. 

Code:

#include <stdio.h>
int main()
{
        int a;
        printf("Enter a number : ");
        scanf("%d", &a);
    
        if (a % 2 == 0) {
            printf("Number is even: %d\n", a);  // Added %d to print the value of 'a'
        } else {
            printf("Number is odd : %d\n", a);  // Added %d to print the value of 'a'
        }
    
        return 0;
}


________________


3. Finding the maximum number between three numbers using C  Programming.


Code:

#include<stdio.h> 
int main() 
{ 
 int a,b,c; 
 printf(“Enter first number: \n “); 
 scanf(“%d”,&a); 
 printf(“Enter second number:\n”);
 scanf(“%d”,&b); 
 printf(“Enter third number:\n”); 
 scanf(“%d”,&c); 
 if(a>b){ 
 if(a>c){ 
 printf(“%d is max\n”,a); 
 } 
 else{ 
 printf(“%d is max\n”, c); 
 } 
 } 
 else{ 
 if(b>c){ 
 printf(“%d is max\n”,b);  } 
 else{ 
 Printf(“%d is max\n”,c);  }
} 
 return 0; 
}  
 
________________


4. Find the factorial of a number using C programming.

Code:


#include <stdio.h> 
int main() 
{
 int i; 
 int fact = 1; 
 int num; 
 printf(“Enter a number:\n”); 
 scanf(“%d” ,&num); 
 for(i=1;i<=num;i++) 
 { 
 fact=fact * i; 
 } 
 printf(“ Factorial of %d is %d\n”, num,fact); 
 return 0; 
} 
 



5. Write a C program to print the multiplication table of a number entered by the user.


Code:

#include <stdio.h>
int main() {
    int number, i;
    // Prompt the user to enter a number
    printf("Enter a number: ");
    scanf("%d", &number);
    printf("\nTable of %d:\n", number);
    // Loop to calculate and display the table
    for (i = 1; i <= 10; i++) {
        printf("%d x %d = %d\n", number, i, number * i);
    }
    return 0;
}

________________


6. Write a C program to display the days of the week by number.

Code:
Echo -e “Enter a day number”;
Read a 
Case $a in
Echo -e “Sunday”;;
Echo -e “Monday”;;
Echo -e “Tuesday”;;
Echo -e “Wednesday”;;
Echo -e “Thursday”;;
Echo -e “Friday”;;
Echo -e “Saturday”;;
Echo -e “Invalid output! Kindly enter number between 1 to 7”;;


________________
Practical 03
Aim: Working with Users, Groups and Permissions.


The commands are as follows:
Step 1: mkdir rewa [make directory]
Step 2:  cd rewa [go to directory]
Step 3:  pwd [path in which directory]
Step 5:  touch text.txt [create file]
Step 6:  touch harry.txt [create file]
Step 7:  ls [list of files]
Step 8:  chmod 700 text.txt [Give permission]
Step 9:  pwd [path of directory]
Step 10:  chmod 764 text.txt [Permissions] 
Step 11:  ls -l text.txt [check permission are done]



Create User
sudo adduser username


Create Group
sudo addgroup groupname


Add User to Group
sudo usermod -aG groupname username


Update Password for User
sudo passwd username


Print User/Group Information
id username
id groupname


Make a Directory
mkdir directoryname


Delete File
rm filename


Delete Directory
rmdir directoryname


Permissions
Read (r): Allows reading the file or listing the directory's contents.
Write (w): Allows modifying the file or directory.
Execute (x): Allows executing the file or accessing contents of the directory.
chmod permissions filename/directoryname
chmod 744 filename/directoryname
________________


Practical 04 
Aim: SSH Server: Password Authentication Configure SSH Server to manage a server from the remote computer, SSH Client (Ubuntu and Windows).

sudo apt-get update

sudo apt-get install openssh-server

sudo systemctl start sshd
sudo systemctl status sshd

ifconfig

Copy the inet address (192.168.29.194) and ping it using Windows CMD
Open cmd in windows and ping the inet address received

Install Putty and click next
configure it using the host name as the inet address received
https://www.chiark.greenend.org.uk/~sgtatham/putty/latest.html
64-bit x86: putty.exe


Hostname (or IP address) → inet address (192.168.29.194) → Open


Put your Ubuntu login credentials
In Login as: name of your username and password(Login using your username (it appears in green in terminal))


Run basic Linux commands in PuTTY
* Ls
* pwd

* First go to root directory using sudo su
* Run tcpdump -e command

(ctrl z- to stop)

________________


Practical 05
Aim: Installation and configuration of FTP server.


Go to root directory
* sudo su

sudo apt update

Install FTP server
* Sudo apt install vsftpd

sudo nano /etc/vsftpd.conf
  

Make the following changes:

Anonymous FTP:
* By default, vsftpd disables anonymous FTP. If you want to enable it, uncomment or add the following line:
LISTEN=YES
anonymous_enable=YES


Chroot Jail:
* To restrict users to their home directories, enable chroot:
chroot_local_user=YES


Local Users:
* To allow local system users to access the FTP server, make sure this line is uncommented:
local_enable=YES


Write Access:
* If you want local users to have write access, uncomment this line:
write_enable=YES

You may restrict.......
chroot_local user=YES

You may specify......
chroot_local_user=YES
allow_writeable_chroot=YES

chroot_list_file=.........


Restart the vsftpd service and check its status
* Sudo systemctl restart vsftpd
* Sudo systemctl status vsftpd


ifconfig


Connect to the inet from the client machine.
* ftp hostname@ipaddress
* ftp riyathakur@192.168.92.129(username@inet)



Write the following commands
* Ls
* cd

ls
cd Desktop
ls
cd ..

Using Windows


Open File Explorer:
* Open File Explorer by pressing Win + E or by clicking on the File Explorer icon in the taskbar.
Access FTP Site:
* In the address bar of File Explorer, type the FTP URL in the following format:
ftp://username:password@ftp_server_address


Replace username, password, and ftp_server_address with your actual FTP server login credentials and server address.





Enter Credentials (if required):
If prompted, enter the username and password for your FTP server.
* Go to file explorer- home- ftp://riyathakur:system@192.168.92.129
* ftp://(username):(password)@(inet)

________________

Practical 06
Aim: Install MySQL to configure database server.
Install XAMPP Server and open it


Start Apache and MySQL


Click on Admin in MySQL


Click on New in the database name and create a new database



Once the database is created, write a code in php to check the database connection.


<?php
$conn = mysqli_connect("localhost", "root", "", "project");


if (!$conn) {
    die("Connection failed: " . mysqli_connect_error());
} else {
    echo "Connection successful!";
}
?>



________________


Practical 07
Aim: Install Samba to share folders and files between Windows and Linux

sudo su
  
cd \

sudo apt-get update


sudo apt install samba


sudo cp /etc/samba/smb.conf /etc/samba/smb.conf.backup  
sudo mkdir -p /home/somaiya/somaiya_TYITH

cd /home/somaiya
  
sudo adduser somaiya


sudo smbpasswd -a somaiya

sudo systemctl restart smbd
sudo systemctl status smbd


sudo nano /etc/samba/smb.conf
  

Add the following lines at the end of the file 
[riya]
Path= /home/Somaiya/riya
Read only= no
Valid users= somaiya


ifconfig




________________


Practical 08
Aim: Install phpMyAdmin to operate MySQL on a web browser from clients.

Go to mysql and Click on Admin in MySQL





Click on New , give the database name and create a new database



For login page : enter table name and columns

Write name and datatype and click on save

The table appears



Once the database is created, Write code on notepad and save in the correct location(go to C drive – ht doc and then there itself create a new folder) with correct extension like .php and all files

------------only login code--------------------

<html>
<head>
<title>My Website</title>
</head>
<body>
<h1>Welcome to my classes</h1>
<form method="post" >
  Name: <input type="text" name="name">
  Email:<input type="text" name="mail">
  Password: <input type="password" name="pass">
  Submit: <input type="submit" name="sb">
</form>
<?php
  $con = mysqli_connect('localhost','root','','test1');
  if(!$con) {
        die("Connection failed: ".mysqli_connect_error());
  }
  if(isset($_POST['sb']))
  {
    $name = $_POST['name'];
    $email = $_POST['mail'];
    $password = $_POST['pass'];
   
    $query = "INSERT INTO mydata10(name, email, password) VALUES ('$name','$email', '$password')";
    $execute = mysqli_query($con,$query);
   
  }


?>
</body>
</html>

Now in browser write: localhost/phpcode and click enter (since php code is the name of the folder you created inside C drive- xampp-htdocs)

Now fill the information and then go to the very first page and click on admin of mysql.


Output:



Now go the database and see if the result is obtained


(*go to services and stop mysql80)


________________


------------------Registration+login page-------------------------------------------------------


<?php
// Database Connection
$con = mysqli_connect('localhost', 'root', '', 'ashwin_registration');
if (!$con) {
    die("Connection failed: " . mysqli_connect_error());
}


// Handle Login Form Submission
if (isset($_POST['login_submit'])) {
    $name = $_POST['name'];
    $email = $_POST['mail'];
    $password = $_POST['pass'];


    $query = "INSERT INTO ashwin_login(Name, Email, Password) VALUES ('$name', '$email', '$password')";
    $execute = mysqli_query($con, $query);


    if ($execute) {
        // Move to Registration Page (Same File)
        $showRegistration = true;
    } else {
        echo "Error: " . mysqli_error($con);
    }
}


// Handle Registration Form Submission
if (isset($_POST['register_submit'])) {
    $student_name = $_POST['student_name'];
    $age = $_POST['age'];
    $course = $_POST['course'];


    $query = "INSERT INTO ashwin_reg(Student_name, Age, Course) VALUES ('$student_name', '$age', '$course')";
    $execute = mysqli_query($con, $query);


    if ($execute) {
        echo "<script>alert('Student Registered Successfully!');</script>";
    } else {
        echo "Error: " . mysqli_error($con);
    }
}
?>


<html>
<head>
    <title>Student Portal</title>
</head>
<body>
    <h1>Welcome to My Classes</h1>


    <?php if (!isset($showRegistration)) { ?>
        <!-- Login Form -->
        <form method="post">
            Name: <input type="text" name="name" required><br>
            Email: <input type="text" name="mail" required><br>
            Password: <input type="password" name="pass" required><br>
            <input type="submit" name="login_submit" value="Login">
        </form>
    <?php } else { ?>
        <!-- Registration Form (Visible After Login) -->
        <h2>Student Registration</h2>
        <form method="post">
            Student Name: <input type="text" name="student_name" required><br>
            Age: <input type="number" name="age" required><br>
            Course: <input type="text" name="course" required><br>
            <input type="submit" name="register_submit" value="Register">
        </form>
    <?php } ?>
</body>
</html>